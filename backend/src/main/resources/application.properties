# ============================================
# Configuration Application - Madagascar 🇲🇬
# ============================================

# Nom de l'application
spring.application.name=gestion-produits-madagascar

# ============================================
# Configuration H2 Database (Développement)
# ============================================
spring.datasource.url=jdbc:h2:mem:produits_db
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA/Hibernate
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ============================================
# Configuration MySQL (Production)
# ============================================
# spring.datasource.url=jdbc:mysql://localhost:3306/gestion_produits_mg?useSSL=false&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=votre_mot_de_passe
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# ============================================
# Configuration PostgreSQL (Production)
# ============================================
# spring.datasource.url=jdbc:postgresql://localhost:5432/gestion_produits_mg
# spring.datasource.username=postgres
# spring.datasource.password=votre_mot_de_passe
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ============================================
# Configuration Server
# ============================================
server.port=8080

# Logging
logging.level.com.madagascar=DEBUG
logging.level.org.springframework.web=INFO

# ============================================
# Configuration CORS (pour React Frontend)
# ============================================
# Voir WebConfig.java pour la configuration CORS
